name: base64_properties_decoder
description: A command-line application to decode base64 .properties values
version: 1.0.0
# repository: https://github.com/my_org/my_repo

environment:
  sdk: ^3.0.0

# Add regular dependencies here.
dependencies:
  args: ^2.4.1
  properties: ^2.0.0
  string_validator: ^1.0.0
  # path: ^1.8.0

dev_dependencies:
  lints: ^2.0.0
  test: ^1.21.0
